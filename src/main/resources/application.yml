spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Product
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: update

api:
  placeholder: https://jsonplaceholder.typicode.com
server:
  port: 8081
